// Faça o mesmo programa do exercício anterior, só que dessa vez usando um while.
#include <stdio.h>

int main() {
    int numero = 1;
    while(numero <= 100) {
        printf("%d\n", numero);
        numero ++;
    }
}